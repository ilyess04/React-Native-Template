import AuthReducer, {
  setCredentials,
  resetCredentials,
} from "./authReducer/AuthReducer";
import SettingsReducer, { setLang } from "./settingsReducer/SettingsReducer";

export {
  AuthReducer,
  setCredentials,
  resetCredentials,
  SettingsReducer,
  setLang,
};
